# MongoDB connection string
MONGO_URI=mongodb://localhost:27017
MONGO_DB_NAME=ocpp_csms

# WebSocket server settings
WS_HOST=0.0.0.0
WS_PORT=9000

# HTTP (FastAPI dashboard) settings
HTTP_HOST=0.0.0.0
HTTP_PORT=8000
